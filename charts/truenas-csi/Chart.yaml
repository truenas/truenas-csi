apiVersion: v2
name: truenas-csi
description: CSI driver for TrueNAS SCALE, providing NFS, iSCSI and NVMe-oF/TCP persistent volumes
type: application
# version is the chart version and appVersion is the driver release it installs.
# Both are updated by `make bump-version`.
version: 1.3.0
appVersion: "1.3.0"
kubeVersion: ">=1.20.0-0"
home: https://github.com/truenas/truenas-csi
sources:
  - https://github.com/truenas/truenas-csi
keywords:
  - csi
  - storage
  - truenas
  - zfs
  - nfs
  - iscsi
  - nvme-of
maintainers:
  - name: TrueNAS
    url: https://github.com/truenas/truenas-csi
annotations:
  artifacthub.io/license: Apache-2.0
